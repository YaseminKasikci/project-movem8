# Mémoire et encodage
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.vfs.watch=false
org.gradle.workers.max=1
org.gradle.parallel=false

# 🔑 Empêcher le daemon Kotlin (compiler dans le processus Gradle)
kotlin.compiler.execution.strategy=in-process

# (Optionnel) Si jamais le daemon Kotlin était utilisé, limite mémoire
kotlin.daemon.jvm.options=-Xmx3g,-XX:MaxMetaspaceSize=1024m
